# ===========================================
# notInstagram Docker Environment Variables
# ===========================================
# Copy this to .env and fill in real values
# Then run: docker compose --env-file .env up

# Amazon S3 config (required for image uploads)
AWS_URL=
S3_BUCKET=
AWS_ACCESS_KEY=
AWS_SECRET_KEY=

# Generate a real secret with:
#   docker compose run --rm web bundle exec rails secret
SECRET_KEY_BASE=change_me_generate_a_real_secret_with_rails_secret
